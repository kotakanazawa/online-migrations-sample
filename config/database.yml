default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: online_migrations_sample_development

test:
  <<: *default
  database: online_migrations_sample_test
